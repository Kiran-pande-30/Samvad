1. Use best NextJs projects practices to build a web application that is fast, scalable, and maintainable.
2. Use `export const` arrow functions instead of `export function`/`export async function` declarations.

